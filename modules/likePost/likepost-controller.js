const LikePostModel = require('./likepost-model');

const isLikePost = async (req, res) => {
    try{
        
    }
    catch (err) {
        next(err);
    }
}

const isUnlikePost = async (req, res) => {
    try {
        
      } catch (err) {
        next(err);
      }
}

module.exports = {
    isLikePost,
    isUnlikePost,
}