const LikeCmtModel = require('./likecmt-model');

const isLikeComment = async (req, res) => {
    try{
        
    }
    catch (err) {
        next(err);
    }
}

const isUnlikeComment = async (req, res) => {
    try {
        
      } catch (err) {
        next(err);
      }
}

module.exports = {
    isLikeComment,
    isUnlikeComment,
}